<Page x:Class="ArmenRestauran.Pages.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Авторизация">

    <Grid>
        <Border Background="White" Width="320" Height="400" CornerRadius="15" 
                VerticalAlignment="Center" HorizontalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Direction="0" Opacity="0.15"/>
            </Border.Effect>

            <StackPanel Margin="30">
                <TextBlock Text="ВХОД" Foreground="#8B0000" FontSize="22" 
                           FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,15"/>

                <Label Content="Логин:" Foreground="#8B0000" FontWeight="SemiBold"/>
                <TextBox x:Name="TbxLogin" Height="30" Margin="0,5,0,10" 
                         VerticalContentAlignment="Center" BorderBrush="#D4AF37"/>

                <Label Content="Пароль:" Foreground="#8B0000" FontWeight="SemiBold"/>
                <PasswordBox x:Name="PbxPassword" Height="30" Margin="0,5,0,20" 
                             VerticalContentAlignment="Center" BorderBrush="#D4AF37"/>

                <Button Content="ВОЙТИ" Height="35" Background="#8B0000" Foreground="Gold" 
                        FontWeight="Bold" BorderThickness="0" Click="BtnLogin_Click"/>

                <Button Content="ПРОСМОТРЕТЬ МЕНЮ БЕЗ ВХОДА" 
                        Height="35" Margin="0,15,0,0"
                        Background="#D4AF37" Foreground="#8B0000" 
                        FontWeight="Bold" BorderThickness="0" 
                        Click="BtnViewMenu_Click"/>

                <Button Content="Нет аккаунта? Регистрация" Margin="0,15,0,0" 
                        Background="Transparent" Foreground="#8B0000" BorderThickness="0"
                        Cursor="Hand" Click="BtnToRegister_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>